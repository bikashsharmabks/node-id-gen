#!/usr/bin/env node

var nodeFlakeGen = require('..');
var redis = require('redis');

var redisClient = redis.createClient();

nodeFlakeGen.init(redisClient);

nodeFlakeGen.generate(function(err, result) {
	console.log(result);
	process.exit(0);
});
